<nav class="navbar navbar-expand-md bg-light border-bottom sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/main"><i class="fa-regular fa-compass"></i> Wander</a>

    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <div class="navbar-nav">
        <a href="/listings" class="nav-link">All Places</a>
        <a href="/listings/new" class="nav-link">Add New</a>
      </div>

      <!-- Right-side auth links -->
      <div class="navbar-nav ms-auto">
        <% if (!currentUser) { %>
          <a href="/signin" class="nav-link">Sign In</a>
        <% } else { %>
          <span class="nav-link disabled">Welcome!</span>
          <a href="/logout" class="nav-link">Sign Out</a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
